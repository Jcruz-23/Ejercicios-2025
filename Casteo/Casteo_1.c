//Escribir un programa que declare e inicialice una variable float n=5/2. 
//Luego imprima el resultado de la división, la misma debería ser 2,5.

#include <stdio.h>

int main(){
    float n = 0;
    n = (float)5/2;
    printf("n: %.1f", n);
}
